# Temporary HTTP-only vhost used for the initial Let's Encrypt HTTP-01 check.
server {
    listen 80;
    listen [::]:80;
    server_name sync.cfls.cyberkunju.com;

    location /.well-known/acme-challenge/ { root /var/www/html; }
    location / { return 404; }
}
